# PRD 输入示例

> 仅为结构示例，不包含真实项目、接口、账号或业务数据。

## 需求编号

REQ-001

## 功能

创建一笔业务预约。

## 角色

已认证的业务用户。

## 主流程

用户提交仓库、预约日期和通知邮箱，系统创建预约记录并返回业务编号。

## 业务规则

- 仓库必须存在且属于当前租户。
- 预约日期不能早于当前业务日期。
- 相同幂等键重复提交不能创建第二条记录。
- 创建成功后数据库状态为待审核。

## 验收条件

- 成功返回业务编号和待审核状态。
- 参数缺失或格式错误返回可识别的业务错误。
- 越权仓库不能被创建。
- 重复请求保持幂等。
